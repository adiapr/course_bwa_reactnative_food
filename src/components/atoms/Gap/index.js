import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const Gap = ({lebar, tinggi}) => {
  return (
    <View style={{ width: lebar, height: tinggi }} />
  )
}

export default Gap;
